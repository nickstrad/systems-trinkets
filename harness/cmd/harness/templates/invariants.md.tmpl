# {{.Pattern}} — invariants

Status: **draft {{.Date}} (TODO: agree with the user)**. Every `check.Invariant`
in `{{.Pattern}}_test.go` cites an ID from this table; a failure points back
here to the primitive that was supposed to guarantee it.

Guide row: *"TODO: the invariant sentence from docs/systems-patterns.md"*.
Primitives: TODO per engine (Valkey …; SQLite …; PostgreSQL …).

| ID | Invariant | Guaranteed by | Kind | Must/should | Status |
|----|-----------|---------------|------|-------------|--------|
| INV-{{.InvPrefix}}-01 | **TODO.** The guide-row invariant from `docs/systems-patterns.md`, stated precisely enough to check on final state. | TODO: the store primitive that provides it, per engine. | concurrency | must | draft |
| INV-{{.InvPrefix}}-02 | **Sequential correctness.** TODO: the contract's request/response rules from one client. | Application code (the contract) on top of the primitive. | contract | must | draft |

Interview answers (docs/suite-authoring.md), recorded so a future session knows why:

1. *Invariant(s) to preserve:* TODO
2. *Primitive per engine:* TODO
3. *Concurrent access:* TODO (→ which concurrency tests)
4. *Crash between steps:* TODO (what must survive a SIGKILL + restart mid-load; tested only against targets with `cmd`)
5. *Retries / duplicates / ordering / expiration / recovery:* TODO
6. *Store vs application convention:* TODO (→ must vs should)
7. *Performance expectations:* TODO (metrics to record; optional `[expect]`)

Metrics recorded (not asserted): TODO.
