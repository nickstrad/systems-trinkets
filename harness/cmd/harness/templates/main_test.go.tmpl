// Suite for the {{.Pattern}} pattern. See CONTRACT.md and INVARIANTS.md.
package {{.Package}}

import (
	"os"
	"testing"

	"systems-trinkets/harness/suitekit"
)

// TestMain opens the results sink for the run and closes it at exit. Once a
// Go reference implementation exists, pass suitekit.InProcess(func() http.Handler
// { … }) so `go test ./...` runs this suite against it instead of skipping.
func TestMain(m *testing.M) { os.Exit(suitekit.Main(m)) }

