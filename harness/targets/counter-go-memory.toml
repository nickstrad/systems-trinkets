# The reference SUT on the in-process map store.
pattern  = "counter"
language = "go"
engine   = "memory"
url      = "http://127.0.0.1:8080"
label    = "example-sut memory (map + mutex)"
cmd = ["go", "run", "./example-sut/cmd/counter", "--addr", "127.0.0.1:8080", "--engine", "memory"]
cwd = ".."
